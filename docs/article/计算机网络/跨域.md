---
title: 跨域问题
author: 滥觞
date: 2020-09-23
categories:
  - 计算机网络
tags:
  - 跨域
---
:::tip
本文是《现代JavaScript》学习笔记，原文可以点击 [这里](https://zh.javascript.info/fetch-crossorigin)
:::
## 什么是跨域
源（origin）—— 域（domain）/端口（port）/协议（protocol）

跨源请求 —— 那些发送到其他域（即使是子域）、协议或端口的请求 —— 需要来自远程端的特殊 header。

## jsonp
script 可以具有任何域的 src，例如 
```js
<script src="http://another.com/…">
```
也可以执行来自任何网站的 script。

```js
// 创建全局函数来接受数据
function gotWeather({ temperature, humidity }) {
  alert(`temperature: ${temperature}, humidity: ${humidity}`);
}

// 创建<script>标签
let script = document.createElement('script');
script.src = `http://another.com/weather.json?callback=gotWeather`;
document.body.append(script);
```
注意：这种方法只能发送简单的请求
- 简单的方法：GET，POST 或 HEAD
- 简单的 header —— 仅允许自定义下列 header：
    - Accept，
    - Accept-Language，
    - Content-Language，
    - Content-Type 的值为 application/x-www-form-urlencoded，multipart/form-data 或 text/plain。


## cros
### 简单请求
简单请求 必须满足下列条件：

- 方法：GET，POST 或 HEAD。
- header —— 我们仅能设置：
- Accept
- Accept-Language
- Content-Language
- Content-Type 的值为 application/x-www-form-urlencoded，multipart/form-data 或 text/plain。

如果一个请求是跨源的，浏览器始终会向其添加 Origin header。

服务器可以检查 Origin，如果同意接受这样的请求，就会在响应中添加一个特殊的 header Access-Control-Allow-Origin。

### 非简单请求
对于非简单的请求，例如PATCH、DELETE及其他

浏览器不会立即发出在过去无法完成的这类请求。即在它发送这类请求前，会先发送“预检（preflight）”请求来请求许可。

预检请求使用 OPTIONS 方法，它没有 body，但是有两个 header：

- Access-Control-Request-Method header 带有非简单请求的方法。
- Access-Control-Request-Headers header 提供一个以逗号分隔的非简单 HTTP-header 列表。

如果服务器同意处理请求，那么它会进行响应，此响应的状态码应该为 200，没有 body，具有 header：

- Access-Control-Allow-Methods 必须具有允许的方法。
- Access-Control-Allow-Headers 必须具有一个允许的 header 列表。
- 另外，header Access-Control-Max-Age 可以指定缓存此权限的秒数。因此，浏览器不是必须为满足给定权限的后续请求发送预检。
<img :src="$withBase('/预检请求.png')" alt="预检请求.png" style="display:block;margin:0 auto">
